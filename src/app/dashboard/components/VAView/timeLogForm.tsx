'use client';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function TimeLogForm() {
  return (
    <div className="time-log-form">
      <h2>Time Log Form (Dummy)</h2>
      <Card>
        <CardHeader>
          <CardTitle>Log Your Time</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Placeholder for time logging form (dummy input fields).</p>
        </CardContent>
      </Card>
    </div>
  );
}